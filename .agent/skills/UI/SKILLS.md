All UI changes must be done using the existing components and following the existing design system.

Our components must be responsive and work on all platforms (web, iOS, Android).

Our ui must be consistent accross our pages.

Use lucide-react-native for icons.

Use the existing color palette and typography.

For our cards, use radius 20px for the corners and must be centered in the page for all platforms, vertically and horizontally. 